{
  "providers": ["node"],
  "phases": {
    "setup": {
      "nixPkgs": ["nodejs_20", "openssl"]
    },
    "install": {
      "cmds": ["npm ci || npm install"]
    },
    "build": {
      "cmds": ["npx prisma generate", "npm run build"]
    }
  },
  "start": {
    "cmd": "npm start"
  }
}
